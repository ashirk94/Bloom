<div class="main-wrapper">
	<h2>Potential friends</h2>
	<div class="friends-container">
		<% for (let friend of locals.friends) { %>
		<div class="friend-box">
			<a href="/user/<%= friend.username %>">
				<img
					class="list-pic"
					src="data:image/<%= friend.profilePic.contentType %>;base64,
        <%= friend.profilePic.data.toString('base64') %>" /></a
			><br />
			<%= friend.firstName %> <br>
            <p class="bio"><%= friend.bio %></p>
			<h5>Interests:</h5>
			<ul>
				<% for (let interest of friend.interests) { %>
				<li><%= interest %></li>
				<% } %>
			</ul>
			<h5>Values:</h5>
			<ul>
				<% for (let value of friend.values) { %>
				<li><%= value %></li>
				<% } %>
			</ul>
            <h5 data-num="<%= friend.likes.length %>" id="<%= friend.id %>"><%= friend.likes.length %> </h5>
            <div class="btn-card-group">
                <button class="btn-card" id="like-btn" data-id="<%= friend.id %>">Like</button><button class="btn-card">Message</button>
            </div>			
		</div>
		<% } %>
	</div>
</div>
<script src="/like.js" defer></script>